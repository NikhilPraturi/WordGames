package com.kanyakubjbrahminsamiti.akhil.wordgames;

import android.content.Intent;
import android.os.Bundle;
import android.support.design.widget.FloatingActionButton;
import android.support.design.widget.Snackbar;
import android.support.v7.app.AppCompatActivity;
import android.support.v7.widget.Toolbar;
import android.view.View;
import android.widget.TextView;
import android.widget.Toast;

public class ResultsPage extends AppCompatActivity {
/*

String gameover;
    TextView userscore,sysscore,winner,score;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_results_page);
        userscore=(TextView)findViewById(R.id.userscore);
        score=(TextView)findViewById(R.id.score);

        sysscore=(TextView)findViewById(R.id.sysscore);
        winner=(TextView)findViewById(R.id.textView12);
        Bundle b=getIntent().getExtras();
        int sys;int user;
        gameover=b.getString("gameover");
        if(gameover!=null)
        {
            sys=Integer.parseInt( b.getString("sys"));
            user=Integer.parseInt(b.getString("user"));
score.setText(gameover);
            sysscore.setText(String.valueOf("System Score:"+ sys));
            userscore.setText(String.valueOf("User Score:"+ user));
        }else {
    try {
        sys = Integer.parseInt(b.getString("sys"));
    }
    catch (Exception e)
    {
        sys=0;
    }
            user = Integer.parseInt(b.getString("user"));

    if (sys!=0) {
        sysscore.setText(String.valueOf("System Score:" + sys));
    }
            userscore.setText(String.valueOf("User Score:" + user));
        }
        if(sys>user)
       {

          // Toast.makeText(ResultsPage.this,String.valueOf(sys)+":sys",Toast.LENGTH_LONG).show();
winner.setText("System Won!!!");
       }else {
            winner.setText("User Won!!!");

        //   Toast.makeText(ResultsPage.this, String.valueOf(user)+":user", Toast.LENGTH_LONG).show();

       }
    }
*/

}
